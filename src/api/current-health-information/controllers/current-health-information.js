'use strict';

/**
 * current-health-information controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::current-health-information.current-health-information');
